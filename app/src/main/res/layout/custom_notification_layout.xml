<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/custom_notification_layout.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/notification_text1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Captura de pantalla realizada"
        android:textSize="16sp"
        android:textColor="@android:color/black"/>

    <TextView
        android:id="@+id/notification_text2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pulsa para ver la captura de pantalla"
        android:textSize="16sp"
        android:textColor="@android:color/black"/>

    <!-- ImageView para la imagen -->
    <ImageView
        android:id="@+id/notification_image"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:scaleType="centerCrop"
        android:src="@drawable/foto_celulas"
        android:contentDescription="Image"/>

    <!-- TextView para el texto -->


</LinearLayout>